# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/gas-station

# Server Configuration
PORT=5001
NODE_ENV=development

# JWT Secret (generate a random string)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server URL for auto-reload (production only)
SERVER_URL=https://your-server-url.onrender.com

# CORS Origins (comma-separated, optional)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,https://gasstation01.netlify.app

